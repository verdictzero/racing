# Hoisting off by default keeps phantom dependencies out of the packages: if a package
# uses something, it has to declare it. That matters most for the air-gapped build, where
# a phantom dep is only discovered once the vendored tarballs are the only source.
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
